import { Grid, Col, Card, Text, Metric } from "@tremor/react";

const Summary = () => {
  return (
    <Grid numItems={1} numItemsSm={2} numItemsLg={3} className="gap-2">
      {/* Widget Components Will Be Injected Here */}
    </Grid>
  );
};

export default Summary;
